"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ksuid_1 = require("./ksuid");
describe("ksuid tests", function () {
    var epoc = (new Date().getTime() / 1000) | 0;
    var k = ksuid_1.default(epoc);
    test("generates a ksuid", function () {
        console.log(k);
        expect(k.length).toBe(26);
        expect(typeof k).toBe("string");
    });
    test("the generate ksiud should be valid", function () {
        var t = ksuid_1.parse(k);
        console.log(t.time);
        expect(t.timestamp).toBe(epoc);
        expect(typeof t.time).toBe("string");
        expect(Buffer.isBuffer(t.payload)).toBe(true);
    });
});
