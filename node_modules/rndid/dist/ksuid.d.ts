/// <reference types="node" />
export interface KType {
    time: string;
    timestamp: number;
    payload: Buffer;
}
declare function parse(uid: string): KType;
/**
 * generate a KSUID
 * @param seconds seconds with with which to generate KSUID
 */
export default function ksuid(seconds?: number): string;
export { parse };
